================================================================================
================================================================================
BUDGET ANNIHILATION — DESIGN / LEAK / PERF ISSUES

This is "issues.txt" for this game "budget-annihilation" (i.e. "rts-prototype").

Your task is to fix the issues documented here.

Errors, Problems, Inconsistencies, TODOs, etc are listed here. As we fix
something we should completely delete it from this file. You should also delete
an item if you know for a fact it has been addressed. Remember that our code may
have changed since the issue was written; check the code and if the issue is no
longer relevant, delete it.

Please always commit and push before and after any code changes we make.

--------------------------------------------------------------------------------
Generated 2026-05-27 by auditing the code against design_philosophy.html.
Each item: [SEVERITY] LOCATION — RULE — PROBLEM / FIX.
File:line refs were correct at audit time; re-verify before editing.
================================================================================
================================================================================


================================================================================
SECTION A — DESIGN-PHILOSOPHY INCONSISTENCIES
================================================================================
Items A1-A17 added 2026-05-29, auditing the code against
budget_design_philosophy.html — universal entity IDs + host hierarchy, shot
source provenance, lock-on exclusion objects on turrets/units/towers,
host-directed vs fully-autonomous turrets, and the tower entity type. Some
original claims have since been fixed in the TypeScript authoritative sim
(runtime turret/locomotion ids, TS EntityMeta registry, ShotSource provenance);
the original A1-A17 refs have all been resolved or moved to VERIFIED-CLEAN. A
follow-on 2026-05-29 emission/id-abstraction audit added A18-A20; those have
also been resolved or moved to VERIFIED-CLEAN.

NB: the host-directed/fully-autonomous turret system and the Tower-as-peer
entity type both audited essentially CLEAN — see VERIFIED-CLEAN at the bottom.

================================================================================
SECTION B — MEMORY LEAKS
================================================================================
(Overall cleanup discipline is strong: fixed-cap instanced pools with destroy(),
balanced listeners/timers, per-id pruning, WebRTC teardown. Only the items below
grow unbounded over a match.)

================================================================================
SECTION C — PERFORMANCE / SCALE (target: 5,000 units)
================================================================================
(All audited items addressed — see VERIFIED-CLEAN.)

================================================================================
SECTION D — WATCH ITEMS (info, not yet actionable)
================================================================================

D1. [WATCH] @dimforge/rapier3d-compat (a 3D physics engine — banned category) is present
    in node_modules as a transitive runtime dependency of @types/three. It is NOT
    imported anywhere in src/ (grep clean), so it is dependency-tree pollution, not a
    code violation — but a full physics engine ships in the lockfile and is one careless
    import away from re-entering. Keep an eye on it; do not import it.


================================================================================
VERIFIED-CLEAN (audited, no action needed — recorded so we don't re-investigate)
================================================================================
- No acceleration on the wire; PREDICT mode is POS/VEL only; no ACC option.
- The four EMA channels (movement pos/vel, rotation pos/vel) are each applied to
  ALL their documented fields (unit transform/velocity, beam point pos/vel, yaw
  scalar + orientation quat + turret yaw/pitch, angularVelocity3 + turret rates,
  force-field range visualizer).
- Travelling projectiles snap on velocityUpdate events (no EMA target populated).
- Delta thresholds: position uses absolute error, velocity uses relative; the
  zero-velocity edge is sent; baseline = last value actually sent per recipient.
- EntityType has Tower as a true peer of Unit/Building; fabricator is a tower, not a
  building; pure-infra buildings carry no turrets/lock-on.
- Turrets name one neutral `shotId`; force-field spheres use a `forceField`
  ShotBlueprint (`forceFieldBarrierShot`) instead of inline turret emission data,
  while panel/sphere projection geometry stays on the mount side.
- Build sites and factory unit shells are the real product entities from spawn:
  `construction.startBuilding` and factory shell creation allocate the final
  EntityId up front, attach `buildable`, and completion removes that component
  without replacing or re-id'ing the entity.
- Lock-on exclusion lists are honored subtractively in targeting for turrets and
  host-level unit/tower lock-on; candidate enumeration covers the
  unit/tower/building/turret-mount families.
- Host-directed vs fully-autonomous turret mounts are fully wired: the tag lives
  on the unit/tower MOUNT entry (TurretMount.hostDirected / BuildingTurretMount.
  hostDirected), NOT on the turret blueprint; the loader throws on !=1 host-directed
  per turret kind; a host lock-on overwrites host-directed turrets while
  fully-autonomous turrets run independently; host-directed mounts fall back to
  autonomous when the host has no lock; and the fight-stop ratio counts only
  host-directed mounts.
- Airborne mount invariant (mount.y===0 && mount.z===0, no zResolver) is enforced and
  throws at startup.
- Rockets inherit the launch target and never re-acquire; lost target → ballistic.
- Sensors are capability sources with fixed radii (not derived from weapon range).
- Resource movement: every stockpile mutation funnels through ResourceMovementSystem
  credit/debit; renderer drives one shared pylon rig for all resource visuals.
- Selection panels are gated on entity TYPE capability flags, not per-blueprint buttons.
- No banned libs (Phaser/Pixi/Matter/cannon/ammo) are imported anywhere in src/.
- No legacy/old/_unused dirs or .old/.bak files in src/ or rts-sim-wasm/.
- Sim-relevant top-level config exports are JSON-backed; remaining TS files are loaders
  or derived display code, not authoritative tuning tables.
- Client unit prediction motion uses a batched Rust/WASM kernel for target extrapolation
  and rendered entity motion; the old TS single-body wrapper path is gone.
- Adaptive terrain mesh build is fully Rust/WASM-owned (C16 closed). One kernel
  `terrain_build_adaptive_mesh` runs the whole host bake — hierarchy refine, 2:1
  balance, conforming T-junction topology, crack-repair loop, vertex-height
  smoothing, triangle neighbor metadata, and the cell->triangle index — sampling the
  same analytic height generator the metal-deposit kernels use. TypeScript is pure
  object assembly (config slice in, TerrainTileMap out); the old TS topology/repair
  code and the granular neighbor/discrepancy/smoothing WASM bridge fallbacks were
  deleted (Delete The Old Path). The 22-value generation-config + 5-stride flat-zone
  packers now live once in terrainGenerationConfig.ts, shared by the mesh baker and
  metalDepositConfig.ts. Cell-index WASM exports stay (client wire-unpack reuses them).
