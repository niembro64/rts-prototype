SCALE-1000 — make 1,000 real units smooth at 30 Hz sim / 60 fps render.
Measured 2026-07-31 (.tmp/performance-bottleneck-suite-observation-coalesced.json):
sim-only stepMs p95 20.3 ms at 421 live units, 41.2 ms at 681, 109.5 ms at
1,455; observed TPS 9.5 / 5.1 / 2.2 against 30. Diagnosis at every cap >=1000:
"simulation, high confidence". GPU is healthy (11.4 ms p95 at 1,000). The
bottleneck is the main-thread sim plus the snapshot round-trip. Work the items
below in order; re-run `npm run performance:bottleneck -- --suite` after each
and append the numbers here. Item numbers continue the pre-e5335070 ledger.

MEASURED 2026-08-12 (.tmp/perf-suite-final-2026-08-12.json, after the audit
fix pass d16112e3..92d92246; note --unit-cap again means TOTAL live units —
the per-player cap change had silently doubled harness load since 08-04, so
07-31 numbers are not directly comparable): sim-only stepMs p95 18.8 at cap
500, 37.6 at 1,000 (~620 live), 129.1 at 2,500, 279.9 at 5,000. Full-stack
frameMs p95 11.1 / 14.9 / 19.1 / 26.2 — cap 1,000 is UNDER the 16.6 ms
budget at the default camera. Biggest movers this pass: shield-field pair
rejection (combat.projectiles 17.0 -> 8.2 ms/tick), entity cone cull in the
scoped packet fill (unit rows 1000 -> ~305 at default camera, frame p95
25.9 -> 14.9), LOD animation shedding re-enabled. New tooling: per-system
sim tick phase timers in every harness run, `npm run build:wasm:profiling`
for symbolicated CPU profiles, --camera-distance harness knob, and
optimization:gate now FAILS against scripts/performanceBaseline.json.
Remaining sim ranking at cap 1,000 (phase timers): combat.projectiles 8.2,
targetingFsm ~6.7 (dominated by combat_targeting_mark_observation_cell —
per-tick vision re-marking), targetingStamp ~5.0 (51-arg JS->WASM stamp),
fireTurrets ~4.9, physics ~3.2 (pool_resolve_sphere_sphere superlinear in
crowding), unitForces ~2.4.

[30]+[32] REWRITTEN 2026-08-12 — the original text is stale: MessagePack
    encode/decode has been off the local path since 06-27/06-29 (direct typed
    materialization; Rust wire encoder ~2 ms even at 5k). What actually
    remains of the snapshot round-trip, in measured order:
    (a) the generic per-entity DTO apply fallback — any entity touching
        builder/factory private state falls off the typed placeholder mask
        (TYPED_PLACEHOLDER_UNIT_DELTA_FIELDS, stateSerializerEntities.ts)
        into the ~230-line generic branch in applyNetworkState; widening the
        mask keeps combat/factory churn on the typed path;
    (b) O(visible) set reconciliation per packet on both ends
        (writeRichDeltaEntityRows diffing vs previousVisibleEntityIds);
    (c) supplemental DTO rebuilds per packet: minimap dominates (its wire-row
        double-write for never-encoding local listeners was removed
        2026-08-12, be1a54bd — the DTO walk itself remains), economy DTOs
        are built then discarded by sharesAuthoritativeState clients (small;
        needs NetworkServerSnapshot.economy to become optional).

[24][25][26][27] Finish slot-native hot loops (existing item text stands):
    Simulation.updateUnits action loop to Rust batches [24]; UnitForceSystem
    pack/scatter removal [25]; PhysicsEngine3D slot-native dirty-range sync
    [26]; spatial queries fully native without JS Entity materialization [27].
    Profile evidence 2026-07-31: findBeamSegmentHit 3.86% self,
    supportSurfaceIndex.addOrUpdateEntity+rebuild 3.81%, applyAreaDamage
    1.11%, GC 1.27%. physics.poolResolveSphereSphere grows superlinearly
    (2.68 ms/call at 1,000 nominal -> 10.59 at 5,000): fix pair-count growth
    (broadphase cell fit + early-out on sleeping pairs) as part of [26]/[27].
    PROGRESS 2026-08-12: [25] force-batch slots/flags/rows/out-flags now live
    in WASM staging (unit_force_step_batch_staged, 525c2f18); the terrain/
    ground sample buffers (4 f64/unit both ways, copied twice per tick via
    terrainSampleGroundForSlots + poolStepIntegrate) still cross by copy.
    [26] per-bucket max-radius rejection landed in all four sphere-resolve
    scan loops (d54d5efb, ~10% count-adjusted at cap 2500; win scales with
    radius spread) — sleeping-pair early-out and cell-fit remain. Also
    landed: isWaterAt WASM fast path (5b21b37c) draining the JS barycentric
    terrain walk from the support-surface probes. Still JS-side per tick:
    supportSurfaceIndex full rebuild TWICE per tick+frame pair (two separate
    instances, sim + render), the 51-arg targeting stamp per combat entity
    (most args already live in the entity_state slab), WorldState.getEntity
    Map lookups where slots are known.

[33] Sim in a Worker over SharedArrayBuffer. Prerequisite LANDED: vite dev+
    preview now send Cross-Origin-Opener-Policy: same-origin and
    Cross-Origin-Embedder-Policy: require-corp, so crossOriginIsolated is true
    and SharedArrayBuffer is available (static hosts need a coi-serviceworker
    shim; Tauri needs the same headers on its protocol handler). Move the
    30 Hz ServerTickLoop + ServerSimulationCore + the WASM instance into a
    dedicated Worker with WASM memory backed by SharedArrayBuffer; main thread
    keeps render + input + Vue and reads presentation slabs through Atomics-
    published tick fences. Do AFTER [30]+[32] (less state to share) and only
    once [24]-[27] shrink the JS<->WASM chatter (fewer per-tick JS objects to
    proxy across the boundary). Render must interpolate from the shared pose
    history exactly as ClientLockstepPresentation does today.

[34] Make the cheap render path real at scale. The 2026-07-31 suite ran with
    renderPhaseUnitLodProxyRows = 0 — the glyph LOD never engaged, so every
    benchmark unit drew a full EntityMesh3D group. (a) Add a zoomed-out camera
    point to the harness matrix so glyph/far rungs are exercised; (b) then
    [31] (existing text stands): render high-count units from stable GPU
    instance buffers with dirty-range updates instead of per-unit Object3D
    orchestration. Success: full-stack frameMs p95 <= 16.6 at 1,000 real units
    at both camera points.
    PROGRESS 2026-08-12: (a) DONE — --camera-distance=N knob (cc465ff5);
    at distance 3500 glyph proxy rows engage (~150 p50 at cap 1,000). The
    close-camera point now meets the <=16.6 target (14.9 p95) via the scoped
    cone cull (71dd0833); the FAR camera point does not (31.5 p95): with the
    whole army in the cone, renderer.info shows ~9.9k draw calls from
    per-object rig meshes (~4 small direct boxes/unit) and HUD sprites —
    that is exactly (b)/[31]. lod.json animation shedding re-enabled
    (unitAnimationMinRung mid, stride 3) — far rigs animate at 1/3 cadence.

[35] NEW 2026-08-12 — Vision observation marking is the top remaining sim
    function: combat_targeting_mark_observation_cell 8.2% self (symbolicated
    profile), re-stamped per source per tick inside combat.targetingFsm.
    Incremental marking needs cross-tick mask persistence with
    move-invalidation — a fog-truth semantics design, not a drive-by (the
    2026-08-12 circle prefilter on the square sweep is in, 20d4f798, neutral
    at unit sight radii). Related cheap follow-ups: slow the candidate
    re-scan cadence for turrets whose target stays valid (BAR parity), and
    shrink the targeting stamp per [24]-[27] note above.
