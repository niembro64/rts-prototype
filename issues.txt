================================================================================
================================================================================
BUDGET ANNIHILATION — DESIGN / LEAK / PERF ISSUES

This is "issues.txt" for this game "budget-annihilation" (i.e. "rts-prototype").

Your task is to fix the issues documented here.

Errors, Problems, Inconsistencies, TODOs, etc are listed here. As we fix
something we should completely delete it from this file. You should also delete
an item if you know for a fact it has been addressed. Remember that our code may
have changed since the issue was written; check the code and if the issue is no
longer relevant, delete it.

Please always commit and push before and after any code changes we make.

--------------------------------------------------------------------------------
Generated 2026-05-27 by auditing the code against design_philosophy.html.
Each item: [SEVERITY] LOCATION — RULE — PROBLEM / FIX.
File:line refs were correct at audit time; re-verify before editing.
================================================================================
================================================================================


================================================================================
SECTION A — DESIGN-PHILOSOPHY INCONSISTENCIES
================================================================================
Items A1-A17 added 2026-05-29, auditing the code against
budget_design_philosophy.html — universal entity IDs + host hierarchy, shot
source provenance, lock-on exclusion objects on turrets/units/towers,
host-directed vs fully-autonomous turrets, and the tower entity type. Some
original claims have since been fixed in the TypeScript authoritative sim
(runtime turret/locomotion ids, TS EntityMeta registry, ShotSource provenance);
the original A1-A17 refs have all been resolved or moved to VERIFIED-CLEAN. A
follow-on 2026-05-29 emission/id-abstraction audit added A18-A20; those have
also been resolved or moved to VERIFIED-CLEAN.

NB: the host-directed/fully-autonomous turret system and the Tower-as-peer
entity type both audited essentially CLEAN — see VERIFIED-CLEAN at the bottom.

--------------------------------------------------------------------------------
Items A21-A36 added 2026-05-30 by a fresh multi-agent audit of the code against
budget_design_philosophy.html (every design-philosophy section). 39 candidate
findings were produced; each was adversarially re-read by an INDEPENDENT verifier
against the live code, and 21 candidates were refuted (auditor misreads of
already-correct code — e.g. the host-directed one-per-kind loader throw, the
level-1 lock-on exclusions, hover anti-gravity, config-is-data, and the resource
pylon channel all re-verified CLEAN; see VERIFIED-CLEAN). The remaining confirmed
item from that audit — the dual TypeScript/Rust authoritative-sim duplication — is filed in
SECTION C as C1. A36 has since been resolved by generating the TypeScript and
Rust blueprint/shot type surfaces from one schema.
File:line refs were correct at audit time; re-verify before editing.

--------------------------------------------------------------------------------
Items A37-A44 added 2026-05-30 alongside a design-direction expansion of
budget_design_philosophy.html (new/edited sections: "Every entity shares one base
ledger", "Entity geometry is three radii", "Pieces have their own health and die
independently", "Every piece is an agent; the player commands only hosts", "Every
death is an explosion", "Shots are destructible bodies too", "Hosts build from the
ground up, one piece at a time", the Atomic Gameplay Pieces split of emissions into
shot/ray/shield (ray = the beam+laser line-emission kind) with "force field"
renamed "shield", and a rewrite of the lock-on eligibility default to "universal
by default, subtractive from there"). A37 has
since been resolved; the remaining items describe where the CODE does not yet meet
the expanded philosophy. They are forward work, not regressions.
File:line refs were correct at audit time; re-verify before editing.

All A37-A44 items are now resolved or verified not currently actionable. The latest
re-read confirmed A39's implemented piece-health, death-explosion, host-death
promotion, force-driven live-detach, and snapshot-liveness paths; no authored
non-blast tear-off source exists in code or blueprint data today.

--------------------------------------------------------------------------------
Items A45-A49 added 2026-05-30 alongside a design-direction expansion of
budget_design_philosophy.html "Resource Movement Flows Through Pylons" (new
sections: "A pylon is a conduit: root, tube, head", "The converter is two tubes
and a taxed arc"; reconciled the tip-relative inbound/outbound definition; enriched
the solar/wind/extractor/construction/converter pylon roster). The model: a pylon
is a TUBE between a ROOT (the player's pool tap) and a TIP/HEAD (the world); balls
DOWN the tube = gaining, UP the tube = spending; the tip CONSERVES — every ball up
the tube exits the tip, every ball into the tip travels down the tube, and balls
are spawned/sunk only at the root and the world point, never at the tip. These
items described where the renderer did not yet meet that model; they were forward
visual work, not regressions, and rode the existing single ResourceMovement channel
(resource balls stay renderer-only / off-wire / anonymous — philosophy ~1747).
A45-A49 have since been resolved by adding root/head conduit paths, physical
producer source points for solar, wind, and extractor pylons, converter taxed
cross-tip arcs with in-flight recolor, and repair/reclaim flows through
builder-mounted resource pylons.
File:line refs were correct at audit time; re-verify before editing.

--------------------------------------------------------------------------------
================================================================================
SECTION B — MEMORY LEAKS
================================================================================
(Overall cleanup discipline is strong: fixed-cap instanced pools with destroy(),
balanced listeners/timers, per-id pruning, WebRTC teardown. The 2026-05-30 audit
found no new unbounded-growth items.)

================================================================================
SECTION C — PERFORMANCE / SCALE (target: 5,000 units)
================================================================================
C1. [MEDIUM] src/game/sim/Simulation.ts (per-tick orchestration: per-command loop +
    updateUnits/updateCombat at lines ~335-453) and the broader src/game/sim/
    TypeScript sim —
    RULE: "TypeScript Coordinates, WASM Computes" (philosophy lines 843-924) + "Delete The
    Old Path / one language per behavior" (lines 2074-2158): per-unit / per-tick control
    loops belong in Rust/WASM; two implementations of one behavior is forbidden. —
    PROBLEM: the authoritative per-tick orchestration (command execution, per-unit update
    loops, combat/economy/construction stepping) still runs in TypeScript and calls into the
    26k-line Rust sim for the heavy numeric kernels. The audit verified the hot numeric work
    IS batched to WASM (collision/splash broadphase, spatial partitioning, unit-motion
    kernel, terrain bake are all WASM-owned — see VERIFIED-CLEAN) and that PhysicsEngine3D
    is a correct thin WASM orchestrator (NOT a Rust-sim duplicate), so this is the
    acknowledged in-transition orchestration layer, not a hot-loop violation. A live
    authority ledger now exists at src/game/sim/AUTHORITY_LEDGER.md so migrations have
    an auditable owner map. The remaining gap is the long-tail migration of TS control flow
    into Rust. —
    FIX: continue migrating per-tick/per-unit authoritative control flow into the Rust sim
    and DELETE the TS counterparts in the same change (Delete The Old Path). Keep
    src/game/sim/AUTHORITY_LEDGER.md current as rows move from TypeScript-orchestrated to
    transitional to Rust/WASM-owned, so there are never two sources of truth. Large multi-step
    effort; lower urgency than the SECTION A items.
    2026-05-31 progress: the deterministic wind oscillator moved from TypeScript to
    rts-sim-wasm, with Rust constants generated from src/windConfig.json and the old TS
    waveform implementation deleted.
    2026-05-31 progress: wind producer-rate aggregation for WindPowerTracker moved
    from a TypeScript Map loop to the Rust/WASM economy_accumulate_player_rates
    batch helper. Producer stockpile crediting and resource movement accounting
    remain TypeScript-orchestrated under this item.
    2026-05-31 progress: shared stockpile credit/debit clamp accounting moved
    from ResourceMovementSystem's TypeScript math into Rust/WASM helpers
    economy_credit_stockpile and economy_debit_stockpile. Resource-movement
    event recording remains TypeScript-side because it feeds renderer pylon
    visuals; converter/factory/construction orchestration remains under C1.
    2026-05-31 progress: converter per-player rate aggregation now reuses the
    Rust/WASM economy_accumulate_player_rates batch helper instead of a
    TypeScript Map, and converter resource-movement source attribution now
    honors the same ON/OFF gate used by converter throughput.
    2026-05-31 progress: converter tax/source/headroom transfer math moved
    from EconomyManager.processConverters into the Rust/WASM
    economy_compute_converter_transfer helper. TypeScript now applies the
    returned movement amounts only for per-entity pylon attribution.
    2026-05-31 progress: solar/wind/extractor producer stockpile crediting
    moved from per-entity TypeScript ResourceMovementSystem.credit calls into
    the Rust/WASM economy_apply_income_credits batch helper. TypeScript now
    packs active producer rows, copies back the resulting player stockpiles,
    and records accepted pylon movements only for renderer attribution.
    2026-05-31 progress: converter stockpile debit/credit application and
    per-converter consumed/output share distribution moved into the Rust/WASM
    economy_apply_converter_transfers batch helper. TypeScript now only packs
    active converter rows, copies back player stockpiles, and records the
    renderer-facing pylon movement events returned by WASM.
    2026-05-31 progress: base energy/metal income stockpile crediting now uses
    the Rust/WASM economy_apply_income_credits batch path too. TypeScript queues
    base-income and producer rows together, copies back stockpile truth, and only
    records renderer-facing resource movement events after WASM applies the
    accepted amounts.
    2026-05-31 progress: construction and repair equal-share resource debit
    allocation moved from energyDistribution.ts TypeScript math into the
    Rust/WASM economy_apply_equal_consumer_debits helper. TypeScript still
    gathers build/repair consumers and records renderer-facing pylon movement
    attribution.
    2026-05-31 progress: construction/repair consumer spend application
    moved from energyDistribution.ts TypeScript math into the Rust/WASM
    construction_apply_consumer_spends helper. Rust now applies buildable
    paid counters, repair HP, factory progress, and per-resource rate
    fractions; TypeScript scatters returned values to entity objects and
    records renderer-facing pylon movement attribution.
    2026-05-31 progress: factory construction-site placement geometry moved
    from factoryConstructionSite.ts TypeScript math into the Rust/WASM
    factory_build_spot kernel. TypeScript now supplies authored footprint/radius
    config plus entity/rally inputs and maps the returned spot; factory shell
    lifecycle and completion orchestration remain under C1.
    2026-05-31 progress: factory shell spawn-overlap distance checks moved
    from FactoryProductionSystem.isBuildSpotBlocked into the Rust/WASM
    factory_build_spot_blocked kernel. TypeScript now only packs live unit and
    building obstacle radii, calls the kernel, and applies the clear/blocked
    result before spawning the shell.
    2026-05-31 progress: construction piece payment reconciliation,
    dependency-gated piece activation, and HP growth moved from
    constructionLifecycle.ts TypeScript loops into the Rust/WASM
    construction_reconcile_and_grow_pieces batch helper. TypeScript now packs
    buildable piece specs, assigns newly materialized sub-entity ids, scatters
    HP/progress results, and still owns shell completion/effects orchestration.
    2026-05-31 progress: factory per-factory production decision planning
    moved from FactoryProductionSystem.update's inline TypeScript branch tree
    into the Rust/WASM factory_plan_production_actions batch helper. TypeScript
    now packs factory/shell state rows and only applies returned actions,
    creates blocked-clear shells, runs shell activation side effects, and marks
    snapshot dirtiness.
    2026-05-31 progress: build/repair/reclaim horizontal target-distance math
    and commander reclaim HP/refund tick math moved into Rust/WASM helpers.
    TypeScript now marshals entity shape/resource fields, applies returned
    HP/stockpile side effects, and still owns command queue/orchestration.
    2026-05-31 progress: shared building active-state ON/OFF timer lifecycle
    moved from updateBuildingActiveStates TypeScript branches into the
    Rust/WASM building_active_state_step_batch helper. TypeScript now packs
    active-state rows, scatters timer/open results, applies producer-rate
    deltas, and marks renderer/network dirty state.
    2026-05-31 progress: idle/waiting flying-unit loiter steering moved from
    Simulation.applyFlyingLoiterThrust TypeScript vector math into the
    Rust/WASM flying_loiter_step_batch helper. TypeScript now stores/clamps
    loiter centers, packs candidate rows, and scatters thrust/turn-sign
    results for UnitForceSystem.
    2026-05-31 progress: stuck-detection body-speed checks, final-waypoint
    settling checks, stuck-counter reset/increment, and replan eligibility
    moved from Simulation.evaluateStuckAndReplan TypeScript math into the
    Rust/WASM stuck_replan_step_batch helper. TypeScript now packs moving
    unit rows and only owns replan budgeting plus Pathfinder side effects.
    2026-05-31 progress: generic move/fight/patrol waypoint completion
    classification moved from Simulation.hasArrivedAtAction TypeScript math
    into the Rust/WASM arrival_completion_step_batch helper. TypeScript now
    batches candidate action rows, then only applies queue advancement,
    flying-loiter handoff, or arrival-thrust side effects from the returned
    decisions.
    2026-05-31 progress: turret yaw/pitch damped-spring integration moved
    from per-turret TypeScript WASM calls into the Rust/WASM
    turret_rotation_step_batch helper. TypeScript still resolves target
    poses, ballistic lock drops, and combat activity-mask refreshes under
    this item.
    2026-05-31 progress: non-packed guided/D-gun projectile
    constant-acceleration integration moved from projectileSystem.ts and
    ClientProjectilePrediction.ts into the Rust/WASM
    projectile_integrate_step_batch helper. D-gun terrain-follow vertical
    thrust also moved into Rust/WASM, and the old TypeScript homing-thrust
    fallback was deleted so guided thrust has one implementation.

================================================================================
SECTION D — WATCH ITEMS (info, not yet actionable)
================================================================================

D1. [WATCH] @dimforge/rapier3d-compat (a 3D physics engine — banned category) appears in
    package.json devDependencies, but it is now wired to a LOCAL NO-OP STUB:
    "@dimforge/rapier3d-compat": "file:vendor/npm-stubs/rapier3d-compat". That stub
    (vendor/npm-stubs/rapier3d-compat/{package.json,index.js,index.d.ts}, marked
    private:true, "Local stub used only to satisfy unused @types/three Rapier
    declarations") contains no engine code. The real physics engine is NOT in the
    lockfile and is NOT imported anywhere in src/ (grep clean). The original concern (a
    full physics engine shipping in the tree) is therefore defused. Keep watching: do not
    swap the stub back for the real package, and do not import it.
    (2026-05-30: previously this item claimed "a full physics engine ships in the
    lockfile" — that is no longer true; commit 11526e73 "Remove transitive Rapier engine
    package" replaced it with the stub.)


================================================================================
OTHER
================================================================================

