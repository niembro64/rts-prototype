This file is called "issues.txt". Errors, Problems, Inconsistences, TODOs, etc will be listed here. As we fix something here we should completely delete it from this file. You should also delete an item if you know for a fact it has been addressed.

#####################################################

  Findings

  ---------------------------------------------------
  6) OUTDATED / STALE COMMENTS
  ---------------------------------------------------

  [MEDIUM] src/game/render3d/Render3DEntities.ts:3475-3480 says "mirrors
    the 2D BuildingRenderer's bottom-up fill" — there is no 2D
    BuildingRenderer in the codebase any more. Also says "scales
    vertically by buildProgress" but the local field is now `progress`
    (avg of three computed from paid/required).

  [MEDIUM] src/types/sim.ts:629-630 — "Economy state per player.
    `resourceCost` on a buildable is the SAME number for energy + mana
    + metal" is wrong post-refactor; per-resource costs are now
    independent (`{ energy, mana, metal }` triple).

  [LOW] src/game/sim/economy.ts:155 — `canAfford(playerId, resourceCost:
    number)` parameter still named `resourceCost`, but it's a single
    scalar applied to EACH resource. Misleading given that
    `ResourceCost` is the type for a triple. Rename to
    `perResourceAmount` or change the signature to take a `ResourceCost`.

  ---------------------------------------------------
  7) CONFIGURATION KNOB FRAGMENTATION
  ---------------------------------------------------

  [HIGH] src/game/render3d/Debris3D.ts:815 hardcodes `const side = r * 2`
    for mirror-panel debris while the live system uses
    `r * MIRROR_PANEL_SIZE_MULT * 2`. With MIRROR_PANEL_SIZE_MULT
    currently > 1, the debris panel is 1/MIRROR_PANEL_SIZE_MULT the
    size of the alive panel — visible mismatch on death. Import
    MIRROR_PANEL_SIZE_MULT from mirrorPanelCache.ts (Debris3D already
    imports MIRROR_ARM_LENGTH_MULT from there).

  [MEDIUM] src/game/render3d/HealthBar3D.ts:32-52 hardcodes
    worldOffsetAbove: 12, lowThreshold: 0.3, fgColorHigh: '#44dd44',
    fgColorLow: '#ff4444', fgColorBuild: '#4488ff'. Bar tunables
    belong in shellConfig.ts (or a sibling barTheme.ts) so all bar
    knobs live together — shell-resource tints are already centralized
    there; HP / build colours are not.

  [MEDIUM] src/game/render3d/NameLabel3D.ts:23-48 STYLE block
    hardcodes worldHeight: 8, worldOffsetAbove: 28, font config,
    stroke widths, worldWidthPerChar: 5. Comment says "Kept local
    rather than in shellConfig because the label is a generic naming
    surface". Fair — but then host the knobs in a dedicated
    nameLabelConfig.ts rather than inside the renderer.

  [MEDIUM] src/game/render3d/MirrorMesh3D.ts:47-48 `ARM_THICKNESS_FRAC`
    = 0.18 and `ARM_PANEL_GAP_FRAC` = 0.035 are mirror-system
    tunables that should sit next to MIRROR_ARM_LENGTH_MULT /
    MIRROR_PANEL_SIZE_MULT in mirrorPanelCache.ts.

  [LOW] Same magic-fraction trio for barrel orbit clamps appears at
    three sites:
      - src/game/render3d/TurretMesh3D.ts:227, 237, 242
        (TURRET_HEIGHT * 0.45 / * 0.35 / * 0.9)
      - src/game/math/BarrelGeometry.ts:197, 199, 204
      - src/game/render3d/HudAnchor.ts:30, 33, 38
    Extract to a single BARREL_ORBIT_CLAMP_FRACTIONS constant or a
    shared helper.

8) the flying particles and the lightning bolts in the force field rendering are unnecessary, remove them. Start with just the sphere, if you can think of another level of detail for "MAX" LOD, try to come up with something interesting.  

9) the names above the commanders seem vertically squished. Make sure that for whatever text length that we have that we don't have squished letters. If there is no clean way to do that, then let's use monospace fixed size font and do the math, but ideally there would be an elegant way to use a non-fixed-sized font. 

10) it is unclear what the following do to define the barrels for this turret type:

      barrelLength: 0.01,
      baseOrbit: 0.00,
      tipOrbit: 2.5,
      depthScale: 0.002,

Figure out a better way to name or define the barrel geometry for this barrel type in the blueprint. 