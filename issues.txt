INFO:
This file is called "issues.txt". Errors, Problems, Inconsistences, TODOs, etc will be listed here. As we fix something here we should completely delete it from this file. You should also delete an item if you know for a fact it has been addressed. Remember that our code may have changed since the issue was written; check the code and if the issue is no longer relevant, delete it.

Please always commit and push before and after any code changes we make. If there was unchecked code before, just commit and push before you start making your edits.

As we go make sure we are not creating duplicate code, we should be reusing code as much as possible. If we find ourselves copy/pasting code, we should stop and refactor to reuse code instead. This is especially important for the new code we are writing, but also applies to existing code. If we see duplicate code in existing files, we should refactor it to be reusable and delete the duplicates. Create new helper files for reusable code if necessary, we prefer small file sizes.

==============================================================================


1. Let's add up to three levels of prediction for any element that accelerates due to force, or moves etc. Basically we always need to send "position" updates to the client, we sometimes need to send "velocity" updates to the client, and sometimes we even want "acceleration" updates sent to the client. Our main objective for this game is to have full F=MA style movement, where we have position, velocity, and acceleration - all driven by acceleration = force / mass. Let's add a section in our "PLAYER CLIENT" bar for physics predictions, where we can turn on/off "velocity" prediction and "acceleration" prediction. This should be some sort of three-way toggle, "POS","VEL","ACC", where "POS" would only use the "position" information from the snaps, "VEL" would use the "position" and "velocity" information, and "ACC" would use the "position", "velocity", and "acceleration" information sent from the backend for the client prediction. This should be for any item that moves via acceleration = force / mass. Really we should be trying to do all movement in this way. Add the buttons, find all offenders, and document the changes made and future changes needed in "issues.txt". 