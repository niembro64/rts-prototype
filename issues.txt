1. "fire explosions" need to have their particle speed proportional to the radius of the explosion hit box, and the particle count should be proportional to the damage dealt. All entities should also have an explosion effect when they die that deal damage to a sphere around them that is something like 1.4x their hit box radius, this applies to that explosion as well - units have "body part explosion" as well as "fire explosions" (that are both purely visual obviously). 

2. Our "missiles" (the faster ones between "rocket" and "missile") doesn't need any fancy visuals or spinning to them, they move fast enough that we really only see the smoke. Let's modify all three levels of detail for the missiles to be just a simple extruded triangle, so that the front and back are simple triangles and the sides are squares that "extrude" that triangle.  

3. We have the ability to see "metal", "energy" and "health" bars above units currently. We want to reduce the clutter so let's remove completely the ability to ever see "energy" and "metal" bars on our units or buildings. Those should only ever be visible regarding our player's high level energy and metal. 

4. Currently all "online" players have an option to "return to lobby" which only the host should control - basically the host should control the game room state, whether the game room is in "setup / lobby" state or "playing" state. No other user should be able to the "game room" state. This will clean things up. Users can always leave the game room with a "exit to home" option, but there should be no option for any other ueser to change the game room state. And also any user that "joins" or "rejoins" a game room will join using that same universal ID or whatever - if a user joins a game room for the first time and the game room is currently in a game, they just automatically become a watching spectator. And if a user joins the game again with the game state being in progress they join in the same "position" that they were in that game before. So I wan to repeat this one more time, a user joining a "game room" that is in a "setup/lobby" state will always be dumped into the "spectators" list. The host is the only one who can move spectators (let's call it "spectators") into a team slot. Then when a "game room" is in a "game active" state, any new user or previous spectator joins as a spectator, and any previous actual player will join again in that same position. 