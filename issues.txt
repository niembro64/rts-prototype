1. Give the commander a more powerful version of the beam turret that it has now.

2. If I hover an enemy unit should it show its radiuses? Should it show info in the info box (bottom left box?)? Currently it only shows info on enemy units if I have one of my own units (entities) selected. Do a deep dive in what info should be shown in different conditions when we hover or select my units, team units, enemy units etc. Look at "Beyond-All-Reason" for the right way to do all o this then implement that here. 

3. Describe where our chat windows should be in our three locations, where the size they should be, when they should appear or disappear (if at all), then implement it. Make good UX/UI decisions, and also base it off of "Beyond-All-Reason" chat in general. 

4. The "converter" does not seem to convert metal to energy or energy to metal. It is meant to do so. These converters should act different than those in "Beyond-All-Reason" in that our "converter" can convert metal->energy or energy->metal depending on whatever is maxed out.

5. Let's actually create sliders just like we have in "Beyond-All-Reason" only for the point at which energy will convert automatically to metal (slider on the energy bar) and where metal will convert automatically to energy (slider on the metal bar). 

6. When I have a group of construction units and I tell them to build something, only one of them actually starts building it. This is different than "Beyond-All-Reason", in that game we can either tell one unit to build a bunch of things, then have a bunch of other units "guard" that unit - OR - we can actually select 10 units and just tell those 10 units to build a queue of things - both should work. Make this elegant. 

7. I do really like the current shape of the construction drones, let's make them 1.6x their current size scaled up proportionally. Also make them have more "construction yellow/black stripes". Also scale up the construction sub and do the same adding more yellow/black stripes. Also the "yellow/black" stripes on the "queen tick" and "queen bee" are really bad now, remove what is currently on them then add a ton of construction stripes on them as well --- on and also add lots of construction stripes on teh "fabricator" and "commander" in interesting spots. 