INFO:
This file is called "issues.txt". Errors, Problems, Inconsistences, TODOs, etc will be listed here. As we fix something here we should completely delete it from this file. You should also delete an item if you know for a fact it has been addressed. Remember that our code may have changed since the issue was written; check the code and if the issue is no longer relevant, delete it.

Please always commit and push before and after any code changes we make. If there was unchecked code before, just commit and push before you start making your edits.

As we go make sure we are not creating duplicate code, we should be reusing code as much as possible. If we find ourselves copy/pasting code, we should stop and refactor to reuse code instead. This is especially important for the new code we are writing, but also applies to existing code. If we see duplicate code in existing files, we should refactor it to be reusable and delete the duplicates. Create new helper files for reusable code if necessary, we prefer small file sizes.

==============================================================================


==============================================================================
 PREDICT (POS / VEL / ACC) — implementation status, 2026-05-13
==============================================================================

A new three-way toggle "PREDICT: POS / VEL / ACC" lives in the PLAYER
CLIENT bar (label PREDICT, group of three buttons). It selects how
aggressively the client extrapolates physics between server snapshots.

The semantics apply to BOTH translational and angular state — angle
gets the same three levels as position:
   translational      angular
   ───────────────    ────────────────────────
   position           rotation (angle)
   velocity           angular velocity (rad/s)
   acceleration       angular acceleration (rad/s²)

  POS — no integration. Render state drifts toward the snapshot value
        via the existing lerp / lerpAngle; no velocity / acceleration
        integration runs on the client this frame. This applies to
        position AND rotation.
  VEL — integrate position from server-reported velocity each frame
        AND integrate rotation from server-reported angular velocity
        each frame; ignore both linear acceleration (no gravity, no
        movement-input thrust, no a*dt² term) and angular acceleration.
  ACC — full F=ma + τ=Iα extrapolation (default; matches old
        behaviour). Integrates v += a*dt and ω += α*dt where the
        respective acceleration data is available.

Stored in localStorage under `player-client-prediction-mode`; default
is `acc` so behaviour for existing users is unchanged.

WIRED (reads getPredictionMode() and gates accordingly):
  - src/game/network/ClientUnitPrediction.ts
      advanceSharedUnitMotionPrediction — gates the entire
      advanceUnitMotionPhysicsMutable call. POS skips; VEL passes
      zero accel + zero gravity to the integrator; ACC unchanged.
      Affects both the entity body AND the snapshot target it drifts
      toward (advanceTargetExtrapolation calls the same helper).
      applyClientCombatExpensivePrediction — gates the per-turret
      `weapon.rotation += weapon.angularVelocity * dt` step.
      POS: skip (rotation snaps via lerpAngle to target).
      VEL: integrate rotation from angularVelocity (correct).
      ACC: ALSO integrate rotation from angularVelocity, BUT see
      the angular-data gap below — the snapshot doesn't carry an
      `angularAcceleration` field today, so VEL and ACC behave
      identically here. To make ACC distinguishable we'd need to
      add `weapon.angularAcceleration` to ServerTarget.turrets and
      run `weapon.angularVelocity += angularAcceleration * dt`
      before the rotation update. The sim itself currently produces
      angularVelocity via a critically-damped spring (turretSystem.ts);
      it does not store an explicit angular acceleration value, so
      surfacing one would require either deriving it from
      (k * (aimYaw - rotation) - c * angularVelocity) at serialize
      time or letting the spring step expose its α output.
  - src/game/network/ClientPredictionStepper.ts
      applyBeamPathPrediction — beam-path point integration
      `tp.x += vx*dt + ax*halfDt²` is gated. POS skips; VEL drops the
      ax*halfDt² and v += a*dt updates; ACC unchanged.
  - src/game/network/ClientProjectilePrediction.ts
      Both the snapshot-target integration (lines ~218-229) and the
      live projectile dead-reckon (lines ~248-258) are gated by mode.
      Gravity is skipped in POS and VEL; position integration is
      skipped in POS only.

ANGULAR DATA GAPS (semantics intended but data missing):
  A. Unit body rotation has NO velocity / acceleration data on the
     wire. ServerTarget carries `rotation` but no `rotationVelocity`
     or `rotationAcceleration`. As a result, POS and VEL behave
     identically for the unit body angle — both effectively snap
     toward target.rotation via lerpAngle each frame because the
     client has nothing to integrate with.
     To honor VEL semantics on the unit body:
       - Add `unit.angularVelocity` to the entity (or derive from the
         heading change in the sim).
       - Serialize it into ServerTarget.rotationVelocity (with a
         per-tier quantization threshold like rotationVelocityThreshold).
       - In applyClientUnitVisualPrediction, when mode != 'pos',
         integrate `entity.transform.rotation += rotationVelocity * dt`
         BEFORE the existing lerpAngle drift.
     To honor ACC semantics on the unit body, additionally serialize
     a `rotationAcceleration` (or angular-jerk) field and integrate
     `rotationVelocity += rotationAcceleration * dt` first.
  B. Turret yaw has angularVelocity but no angularAcceleration
     (see the inline note in the WIRED section above). VEL == ACC
     today for turret yaw.
  C. Turret pitch is currently lerp-only on the client
     (`weapon.pitch = lerpAngle(weapon.pitch, tw.pitch, rotPosDrift)`)
     with no pitch velocity on the wire — same gap as the unit body.
     To honor VEL/ACC for pitch, mirror the (A) plan with
     `weapon.pitchVelocity` and (optionally) `pitchAcceleration`.

NOT YET WIRED (future work — not gated; runs at full ACC always):
  1. ClientProjectilePrediction.ts → applyClientProjectileHoming.
     The intercept solver (`solveKinematicIntercept`) seeds the target
     state with the homing target's reported acceleration. In VEL mode
     we should pass accel=0 to the solver; in POS mode we should
     probably skip homing steering entirely so the projectile flies
     ballistically.
  2. unitSuspension / chassis tilt EMA. `advanceUnitSuspension` runs
     each frame regardless of PREDICT mode — it's a visual smoothing
     effect, not an authoritative integration, so leaving it alone is
     defensible, but a strict reading of "POS = no client integration"
     would freeze it too.
  3. Server-side integrators that COULD be gated by snapshot
     emission rate (i.e. only send velocity/acceleration when needed).
     stateSerializer.ts always writes velocity/acceleration into every
     snapshot today; a future per-recipient PREDICT-aware serializer
     could omit fields when the recipient is in POS/VEL mode and save
     bandwidth. Not done — would require client→server PREDICT mode
     announcement.
  4. Pure client-side effects that are NOT server-predicted and so
     have no "snapshot to snap to" (deliberately out of scope):
       - src/game/render3d/Debris3D.ts (gravity-driven debris particles)
       - src/game/render3d/ProjectileRangeEnvelope3D.ts (range preview)
     POS mode would freeze them in mid-air; VEL would strip gravity
     and they'd float away. These are authoritatively-client effects,
     not predictions of server state, so the PREDICT toggle leaves
     them at their full F=ma integration. Document this distinction in
     any future "purely client physics" toggle if one is wanted.
  5. Audit pass for any sim-side movement that bypasses the shared
     advanceUnitMotionPhysicsMutable helper. The audit found unit
     bodies, ballistic projectiles, and beam-path points all flow
     through the shared integrators, but a follow-up grep for `+= .* dt`
     across src/game/sim could surface stragglers (jump arcs, knockback
     impulses, force-field push, etc.) that the PREDICT mode should
     also gate when added.