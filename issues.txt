

	1. Regarding our "radar blip" circles (that show us where enemy units are in radar but that we can't see directly) let's have these fade in and fade out just like enemy entities fade in and fade out, use the same mechanics for these "radar blip" items to fade in and fade out as the other stuff. Let's also make our radar blips purely black on the outside and white dot on the inside. 



----




	2. We need a "long range plasma cannon" called "helios" implemented. It can use the strongest plasma shot we have now but should go very far, but aiming should be slow. 


----


	3. Build spray can't be seen through the water, it would be nice to be able to see build spray not be hidden by water surface. 





----



	4. Make the "duck" unit look better. Make it a bit larger and make the weapon look better. Let's give it a slightly less powerful version of the beam. 

----
	
	5. Make the "eagle" unit only have one thruster, currently it has two, which is double the amount of smoke which is wasteful. 

----

	6. Make the "albatros" unit have two cannons on its wings and move the thrusters to behind the cannons on the wings instead of at the tail end of the unit. So the wings will have the cannons on them and the thrusters on them. 



----


7. Make the "Queen Tick" have only 4 legs, but make them very very thick.

----

8. Make the "daddy" and "widow" have 8 legs.




----

9. In "Beyond-All-Reason", if I have one or many construction units selected, normally if I hover over a metal deposit, it will propose a metal extrator to be built. Let's implement that here as well. Look in "Beyond-All-Reason" for how this works and do it here as well. 

----


10. The "orca" unit shooting still seems to be broken, you need to look deep into the torpedo to see what is gooing on and fix it. 


----

11. The "bot" units seem to be "walking" when they are picked up by the "transport drone", Make sure that all "locomotion" animations are correct for whatever is happening. 
----

12. The "transport drone" let's call the beam a "tractor beam" or "gravity beam" that holds it. It should not look like build spray. It should be a solid transparent volume. The volume should always fill the inner cylinder of the "transport drone" itself and it should animate to "grab" a unit and animate to let them go as well. The shape should never "snap" to any shape, it should animate smoothly, it can appear as a sort of "blob". 
