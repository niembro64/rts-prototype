1. Rework our finite state machine to have the "DEMO BATTLE" area be called "HOME" and this "HOME" will show the demo battle, all lobbies, and the ability to join or create a new lobby like we have now. I just realized that there should have three top level states: "HOME", "GAME ROOM", "ENTITY LAB", and "GAME CONTROLS". The "GAME ROOM" should have sub-states in it for "LOBBY", "BATTLE". The "BATTLE" should have states like "LOADING" and "PLAYING". This is all just ideas for you, look at "Beyond-All-Reason" for some other references on how to structure the states and transitions. We want this stuff very clean and FSM driven in a single location. 

2. We need to have a global chat in teh "demo battle" screen, a "lobby chat" in the lobby that anyone can see and participate in if they are in the lobby, and "team chat" and "all chat" in the battle screen. The "team chat" should only be visible to members of the same team, while the "all chat" should be visible to everyone in the battle including the spectators. Look at how "Beyond-All-Reason" does their chat system for reference on visuals as well. Make sure new messages are displayed and not hidden behind a scroll bar. Also let's delete the "DRAW"/"LABEL"/"ERASE"/"CLEAR" stuff for now. Make chat look and function like "BAR".

3. A spectator of an online game should be able to see the whole map. Turn off our "FOG OF WAR" system for spectators. And let them join at any moment. The spectator seems to think he is player 1 currently. 

4. "GAME ROOM"s still seem visible in our list for a long time even if the host is gone. 

5. When I click "ZZ" with my commander to select all metal deposits in a region to create metal extractors on, it always selects the metal deposit at the center of the map, even though that is not in the circle I selected.  Look again at how this feature is meant to be implemented in "Beyond-All-Reason".

6. Make sure all build spray sprays at the correct place - which would be centered at the center of the item he is trying to build and/or at the whole "collision" volume of that item. Right now spray is not necessarily doing that. 

7. Our function that checks to make sure that we don't have any "Beyond-All-Reason" assets in our game is not working (or has never been run?). I can see that we currently have some "Beyond-All-Reason" assets in our game, but I don't want to manually remove them, we need to cleanly update our system such that it can catch that stuff automatically; probably a combination of file names and file hashes. We need to make sure that we are not using any "Beyond-All-Reason" assets in our game, and that our system can automatically detect and remove them if they are present.

8. Create a file called "things_happening_on_every_tick.txt" and put in it all the things that are happening on every tick of the game. This will help us identify performance issues and optimize our game loop. At the top of each item give your opinion on whether this should or should not run every single tick. 