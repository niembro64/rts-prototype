INFO:
This file is called "issues.txt". Errors, Problems, Inconsistences, TODOs, etc will be listed here. As we fix something here we should completely delete it from this file. You should also delete an item if you know for a fact it has been addressed. Remember that our code may have changed since the issue was written; check the code and if the issue is no longer relevant, delete it.

Please always commit and push before and after any code changes we make. If there was unchecked code before, just commit and push before you start making your edits.

As we go make sure we are not creating duplicate code, we should be reusing code as much as possible. If we find ourselves copy/pasting code, we should stop and refactor to reuse code instead. This is especially important for the new code we are writing, but also applies to existing code. If we see duplicate code in existing files, we should refactor it to be reusable and delete the duplicates. Create new helper files for reusable code if necessary, we prefer small file sizes.

Each item in this file has a unique ID. IDs are stable: once assigned, an
ID is never reused for a different issue, even after the original is
deleted. When you split or expand an item, give the children new IDs in
the same family rather than reusing the parent's ID.

==============================================================================

DESIGN PHILOSOPHY GAP PLAN

==============================================================================

AIMING / TARGETING SYSTEM (AIM-08 family)

------------------------------------------------------------------------------
AIM-08 - parent: aiming/targeting hot paths still keep a TS object writeback
         bridge alongside the Rust slab.
------------------------------------------------------------------------------

Evidence:
- design_philosophy.html sections "Scale Target", "Browser Performance
  Rules", and "TypeScript Coordinates, WASM Computes" call out targeting
  batches, per-unit loops, projectile stepping, and spatial queries as
  Rust/WASM migration targets running over typed memory.
- The targeting FSM port itself is complete: the per-armed-entity
  scheduler (mount kinematics, existing-lock re-validation, candidate
  scan, fire-gate dispatch, state transitions, LOS-grace decay) runs as
  a single combat_targeting_schedule_and_tick_batch Rust call. JS
  consumers (rendering, firing, snapshot encode, force-field engagement,
  movement-halt checks, barrel-spin, beam audio, mirror-target priority,
  aim-solver mount kinematics, client prediction) are slab-first; the
  JS Turret target/state writeback has been deleted, and the mask
  refresh is Rust-owned.
- The transitional residue lives in src/game/sim/combat/
  targetingInputStamping.ts::writeBackCombatTargetingEntity and in the
  mid-tick lock-drop path. Items AIM-08.6 - AIM-08.9 below are the
  concrete remaining sub-tasks; each is independently shippable. When
  all four are done, writeBackCombatTargetingEntity collapses to a
  single Rust call per processed entity (or is removed entirely and
  the scheduler exposes the activeTurretMask !== 0 result directly).

Why it matters:
The current writeback still scales as a per-turret JS object loop on
every armed entity every tick. Tolerable at prototype scale; not aligned
with the 5,000-unit target.

Fix direction:
Each AIM-08.x below = its own PR. Each PR migrates one bridge component
and deletes the predecessor in the same change (see "Delete The Old
Path"). No "we will come back to it later" parity flags.

------------------------------------------------------------------------------
AIM-08.6 - beam inverse target index sync still runs per turret in JS
           writeback.
------------------------------------------------------------------------------

Where:
- src/game/sim/combat/targetingInputStamping.ts::
  writeBackCombatTargetingEntity (per-turret loop calling
  syncBeamWeaponTargetIndex).
- src/game/sim/combat/targetIndex.ts owns the JS-side
  target -> beam-weapon mapping.

What:
Every tick, for every turret on every alive armed entity, JS reads the
new slab targetId and updates the beam inverse index so death handlers
can answer "which beams were aimed at me?" in O(k). The index is a JS
Map keyed on entity id.

Fix:
Move the inverse-index maintenance inside the Rust targeting kernel so
the mapping updates in place whenever the kernel writes a target. JS
keeps a read-only view and consults it only during death cleanup.

Done when:
writeBackCombatTargetingEntity no longer calls syncBeamWeaponTargetIndex
per turret; the Rust kernel owns the index; the JS targetIndex.ts
becomes a read-only wrapper around slab pointers (or is deleted if no
JS reader survives).

------------------------------------------------------------------------------
AIM-08.7 - disabled-turret JS-only field reset still runs every tick.
------------------------------------------------------------------------------

Where:
- src/game/sim/combat/targetingInputStamping.ts::
  writeBackCombatTargetingEntity (per-turret loop calling
  resetDisabledTurretJsOnlyFields when weaponSystemDisabled is true).

What:
For every turret with weaponSystemDisabled === true, the per-tick
writeback zeros angular/pitch velocity + acceleration, burst.remaining,
and forceField.transition/range. These are JS-only Turret fields the
kernel never sees; the slab-side reset is already done by the
scheduler's reset_disabled_weapons pass. Running the reset every tick
does no harm but wastes a per-turret loop iteration on entities whose
disabled-set never changes.

Fix:
Move the JS-only reset to a transition-time helper invoked only at the
moments weaponSystemDisabled actually flips from false to true:
- GameServer mirror enable/disable toggle path.
- GameServer force-field enable/disable toggle path.
- Per-turret config.visualOnly changes (currently startup-only).

Done when:
writeBackCombatTargetingEntity no longer calls
resetDisabledTurretJsOnlyFields; only the disable-transition paths call
it, and weaponSystemDisabled stops being read in the per-tick loop.

------------------------------------------------------------------------------
AIM-08.8 - combat.activeTurretMask / combat.firingTurretMask are still
           mirrored from the slab every tick.
------------------------------------------------------------------------------

Where:
- src/game/sim/combat/targetingInputStamping.ts::
  writeBackCombatTargetingEntity (final block: reads
  views.activeTurretMask[slot] / views.firingTurretMask[slot] into
  combat.activeTurretMask / combat.firingTurretMask).

What:
The Rust kernel owns the masks on the slab. The JS mirror exists for
transitional readers that still touch combat.activeTurretMask /
combat.firingTurretMask directly instead of calling
readActiveTurretMaskForUnit / readFiringTurretMaskForUnit.

Fix:
Repoint every remaining direct reader of combat.activeTurretMask /
combat.firingTurretMask to the slab-first readers in
combatActivitySlab.ts (slab-first with combat.* fallback for non-sim
paths). Then drop the JS mirror; the writeback's final block becomes
just the refreshActivityMasksForEntity call and the activeMask !== 0
return.

Done when:
No sim-path reader reads combat.activeTurretMask /
combat.firingTurretMask; writeBackCombatTargetingEntity stops mirroring
the masks; the non-sim fallback in the slab-first readers either
disappears or is documented as remote-client-only.

------------------------------------------------------------------------------
AIM-08.9 - dropTurretLockMidTick still writes JS Turret.target /
           Turret.state.
------------------------------------------------------------------------------

Where:
- The mid-tick mutation helper clears both the slab and the JS Turret
  in one call. The slab-side clear is necessary. The JS-side write
  exists because legacy activity-mask code read JS Turret.target /
  Turret.state.
- Callers include turretSystem mid-tick lock drops, projectileSystem
  fire-time invalidations, forceFieldTurret transitions, and death
  cleanup paths.

What:
AIM-08.5 moved the activity-mask kernel to read slab state directly,
so the JS Turret target/state writes inside dropTurretLockMidTick may
now be unnecessary. The blob's earlier note ("JS Turret.target /
Turret.state on the sim hot path therefore drift away from the slab
between dropTurretLockMidTick calls; that is fine because no slab-first
reader reads them") suggests the writes are already redundant for
in-sim consumers, but no audit has confirmed it.

Fix:
Audit every consumer of Turret.target / Turret.state across the
codebase. For each one, confirm it is slab-first or non-sim only.
Then drop the JS Turret writes from dropTurretLockMidTick and keep
only the slab clear.

Done when:
dropTurretLockMidTick clears slab only; no reader treats the JS fields
as authoritative on the sim hot path; ideally the JS Turret.target /
Turret.state fields themselves can be deleted in the same change (with
Turret.aimErrorYaw/Pitch as the lone JS-local exception, since
updateTurretRotation and isWeaponAimedForFire use them within the same
tick without crossing the slab).

------------------------------------------------------------------------------
AIM-08.10 - writeBackCombatTargetingEntity should disappear once
            AIM-08.6 / .7 / .8 land.
------------------------------------------------------------------------------

What:
With the per-turret beam-index sync (.6), disabled-field reset (.7),
and JS mask mirror (.8) gone, the writeback function is just
refreshActivityMasksForEntity + read activeMask. That work belongs
inside the scheduler kernel itself - it already knows which slots it
processed.

Fix:
Extend combat_targeting_schedule_and_tick_batch to refresh activity
masks for every processed entity as part of the same batch, and return
an active-set bitmask (or processed-entity flag array) for the JS-side
"does this entity still need rotation/fire integration?" gate. Then
delete writeBackCombatTargetingEntity entirely.

Done when:
targetingSchedulerBridge.ts has no JS bridge loop after the schedule
call; the activity-mask refresh is a Rust kernel responsibility; the
"hasActiveTurretWork" decision comes from the scheduler return value,
not a per-entity post-pass.
