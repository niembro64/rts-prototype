================================================================================
BUDGET ANNIHILATION — ISSUES

This is "issues.txt" for this game "budget-annihilation" (i.e. "rts-prototype").

Your task is to fix the issues documented here.

Errors, Problems, Inconsistencies, TODOs, etc are listed here. As we fix
something we should completely delete it from this file. You should also delete
an item if you know for a fact it has been addressed. Remember that our code may
have changed since the issue was written; check the code and if the issue is no
longer relevant, delete it.

Do not document "PROGRESS" in this file, just document or update issues that need to be resolved. If you see any "progress" information in this file, just remove it, or convert it into something actionable. Just documenting progress is not helpful - update the issue with the current state and the required fixes for it.

Please always commit and push before and after any code changes we make.
================================================================================

1.0 Turret host-integration wire cleanup is incomplete.

Remove the legacy turret hpCurr snapshot slot and related Rust/TypeScript
serializer compatibility code once the wire format can change cleanly. Add
focused coverage that mounted turrets are addressable/lockable but do not carry
independent cost, mass, health, death explosions, construction pieces, detached
agent state, or hostless lifetime; damage through a turret hitbox should resolve
to the host body, and host construction/death should keep the authored turret set
part of the same assembly. (Related: the hp:1 sentinel at
src/game/sim/runtimeTurrets.ts:55 is documented compat, not a separate bug.)

================================================================================
PERFORMANCE — SIMULATION (authoritative tick path)
================================================================================

================================================================================
PERFORMANCE — RENDERER (per-frame hot path)
================================================================================

Main per-frame chain: ThreeApp rAF -> RtsScene3DRenderPhase.run ->
Render3DEntities.update -> updateUnitMeshes (per-unit) -> locomotion/turret/HUD.

================================================================================
PERFORMANCE — SERVER / PHYSICS
================================================================================

5.0 [MAJOR] Per-body force application + idle/hover/boundary math still runs as
per-tick TypeScript loops with per-unit allocation.

src/game/server/UnitForceSystem.ts:89-581 (applyForces) is a per-entity body loop
over dynamicBodies each tick that calls getLocomotionForceProfile(...) per unit per
tick (allocates a profile object — :292,:401). src/game/server/PhysicsEngine3D.ts
does three separate full dynamicBodies scans per tick (clearDynamicSurfaceContacts:757,
the integrate gather, accumulator clear :740) plus collectAwakeEntityIds:607 full-scans
every dynamic body; applyMapBoundaryForces:974 is another per-body spring loop. Body
integration/contacts are already Rust-owned, but force application is not. Violates
"TypeScript must never own per-unit update loops / physics integration" and "do not
allocate in per-unit loops."
FIX: migrate force-application math into a packed WASM kernel (the hover-orientation
batch at PhysicsEngine3D :532 is the model). Meanwhile hoist getLocomotionForceProfile
to write into a reused struct, and fold the three step() scans into one pass.

5.1 [MAJOR] Tick loop drifts; no fixed-timestep accumulator/catch-up; no budget
saturation metric.

src/game/server/ServerTickLoop.ts:13 is a bare setInterval(cb, 1000/hz). GameServer.tick
(:455) clamps per-tick dt to MAX_TICK_DT_MS (good vs spiral-of-death) but there is no
accumulator and no catch-up: a 200ms hitch advances sim by one clamped step, so the
authoritative clock silently slips behind real time and never recovers. TPS/CPU are
measured, but tick-work-ms vs interval-ms saturation is not, so a saturated host can't
tell it's falling behind. Violates "Measure TPS, CPU budget, ... backpressure
continuously."
FIX: drive the loop with a fixed-dt accumulator + bounded catch-up (max N steps/frame),
and surface a tickMs/intervalMs saturation metric into serverMeta.

5.4 [MINOR] Sim-slot guard ("a second sim is a hard error") only fires at tick start.

acquireSimSlot is called in GameServer.start() (:310), but create() (:169) and the
PhysicsEngine3D ctor (:419) already allocate the WASM engine handle and spawn bodies into
the SHARED module-level BodyPool before start() runs the singleton check. Today the old
server is always stopped synchronously first, so it works, but a future create-before-stop
caller would corrupt the shared pool instead of getting the intended immediate throw.
FIX: acquire the sim slot in create()/the PhysicsEngine3D ctor so allocation into the
shared pool is what throws; release on stop()/dispose().

================================================================================
NETWORKING / SNAPSHOTS
================================================================================

6.0 [CRITICAL] Backpressure-dropped delta still advanced the per-recipient baseline
-> permanent desync.

The per-recipient delta baseline is committed inside serializeGameState
(copySentPrevState + captureToRustBaseline, stateSerializer.ts:430/472) which runs in
ServerSnapshotPublisher.emit (:256) BEFORE the bytes reach the transport. The transport
then drops the snapshot for backpressure (NetworkSnapshotTransport.ts:64/200/220/244 via
shouldDropForBackpressure checking dc.bufferedAmount, :308) or for pending FULLSNAP
compression (:325) — but nothing rolls back the baseline or forces a keyframe. The channel
is reliable, so the dropped delta is gone, yet the next delta is diffed against state the
client never received. That recipient's view stays corrupt until an unrelated keyframe
happens to repaint the affected fields (fields that stop changing never repaint). Violates
"DIFFSNAP baseline must advance per-field from the last value actually SENT to that
recipient."
FIX: on any backpressure/pending-compression drop, mark that recipient for a forced
keyframe (snapshotPublisher.forceNextKeyframe for that listener), or defer the baseline
commit until the send is confirmed.

6.1 [HIGH] cloneNetworkSnapshot() allocates a fresh cloner per call on the receive
hot path.

src/game/network/snapshotClone.ts:548 — the free function does
`new ReusableNetworkSnapshotCloner().clone(state)`. That class is designed to be held and
reused (its sub-DTO arrays/objects pool across clones), and good callers hold it
(SnapshotBuffer.ts:36, LocalGameConnection.ts:28). But NetworkSnapshotTransport.storePendingState
(:160) and RemoteGameConnection.ts:39 call the free function, so every buffered client
snapshot (5-10Hz + impairment path) reallocates the cloner and all its internal arrays,
defeating the pool. Violates "Pool snapshot DTOs."
FIX: give NetworkSnapshotTransport and RemoteGameConnection a long-lived
ReusableNetworkSnapshotCloner field and call .clone() on it.

6.2 [MEDIUM] Per-unit build DTO allocated in the serialize loop (unit path only).

src/game/network/stateSerializerEntities.ts:719-728 — when ENTITY_CHANGED_BUILDING is set
for a unit under construction, it allocates u.build = { complete, interrupted, paid:{...} }
(two objects) per unit per snapshot. The building path right below (:800-813) correctly
reuses the pooled poolEntry.buildingBuild.
FIX: add a pooled unitBuild to PooledEntry (mirroring buildingBuild) and write into it.

6.3 [MEDIUM] Client receive-coalescing silently drops a delta when one is already
pending.

src/game/network/NetworkSnapshotTransport.ts:156-162 — storePendingState only stores when
there is no pending state (or when upgrading delta->full); a second delta arriving before
the first is consumed is dropped, not coalesced. Deltas are non-idempotent (each carries
only fields changed since last sent), so dropping one loses those corrections with no
resync trigger.
FIX: apply/merge the pending delta before overwriting, or at minimum request a keyframe
when a delta is dropped here.

6.4 [MINOR] Per-emit allocations break the otherwise-strict pooling discipline.

ServerSnapshotPublisher.ts:191-193 allocates three Maps (teamAudioCache/teamSprayCache/
teamMinimapCache) + a visibility cache (:183) every emit (~10Hz); snapshotRustWireEncoder.ts:2177
allocates `const rawTopLevelKeys: string[] = []` per encode. Once-per-snapshot, not
per-entity, so small.
FIX: hoist to instance fields and .clear()/.length=0 per emit.

================================================================================
ARCHITECTURE / PHILOSOPHY MIGRATION (budget_design_philosophy.html)
================================================================================

7.0 [MAJOR, migration surface] Projectile + damage/death authority is still
TypeScript-orchestrated.

Per AUTHORITY_LEDGER.md (TypeScript-Orchestrated rows), the entire
checkProjectileCollisions / _updateTravelingProjectilesJS path
(combat/ProjectileCollisionHandler.ts, combat/projectileSystem.ts:916) and the
damage/death loop (damage/DamageSystem.ts, Simulation.ts:580 updateCombat / :771
cleanupDeadEntities / :897 detonateEntityDeathExplosions) run per-projectile /
per-hit-entity in TypeScript. Position integration is batched into Rust, but collision
detection, reflection, submunition spawn, termination, detonation, and HP write-back are
not. Homing also makes one solveKinematicIntercept + one computeHomingThrust WASM call
per homing projectile per tick rather than a batch (the TS fns are thin Rust wrappers —
NOT duplicated math, so no Delete-The-Old-Path violation, just un-batched). Violates
"TypeScript Coordinates, WASM Computes."
FIX (C1 migration): move the swept-collision + termination loop into a Rust projectile
kernel; batch the homing intercept/thrust like the existing projectileIntegrateStepBatch.
Combine with 3.0.

7.2 [LOW] Config-as-data residue (gameplay logic/data trapped in TypeScript).

- src/game/sim/turretConfigs.ts:35 — hardcoded
  `if (config.turretBlueprintId === 'turretMortarDrop') return config.range;` — a gameplay
  special-case as a magic string in code instead of a blueprint flag (e.g.
  ballisticRangeIsAuthored). Move to JSON.
- src/game/sim/buildConfigs.ts:32-40 — BUILDING_CONFIGS hand-enumerates the 8 building IDs
  instead of iterating BUILDING_BLUEPRINTS keys (data duplicated in code; also an SSoT
  concern). Iterate the JSON keys.

7.3 [LOW] Delete-The-Old-Path residue.

- src/game/network/snapshotEntityWirePack.ts — legacy V1 unpackEntityRow and the array-form
  unpack* paths allocate fresh objects/createEmptyUnitSub() spreads (e.g. :1194) instead of
  using the V5/V6 decode pools. Current wire version is V6; remove these once no legacy
  producers remain.

7.4 [LOW] Stale 'unit' | 'building' type unions collapse towers into building.

src/types/network.ts:632 and src/types/ui.ts:128 (minimap entity), and
src/game/sim-wasm/snapshotEncoderTest.ts:182,1293 use type:'unit'|'building', diverging from
canonical EntityType = 'unit'|'tower'|'building'|'shot' (sim.ts:911). Cosmetically harmless
on the minimap today but the union is wrong.
FIX: reconcile these unions to EntityType.

7.5 [LOW / design decision] ray/shield exist as blueprint families but not runtime
EntityType kinds.

blueprints/rays.json (beam) and blueprints/shields.json are distinct families and shields
get a turret-mounted runtime, but ray/shield are not entries in the Rust EntityType/KIND
enum (only unit/tower/building/shot/turret). This is consistent with the philosophy (rays
and shields are emitter-bound sustained emissions, NOT bodies, and attribute through their
turret rather than minting an id) — so likely correct as-is. Flag only if a future design
needs to point at an individual ray/shield instance by id.
