// Core runtime entity identifiers.

export type EntityId = number;
export type PlayerId = number;

/** Numeric sentinel for hot-loop entity references with no target. */
export const NO_ENTITY_ID: EntityId = -1;
