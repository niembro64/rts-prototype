// GameConnection types - uniform interface for host and remote clients

export type {
  AuthoritativeRenderSource,
  GameConnection,
  SnapshotCallback,
  SnapshotUnsubscribe,
  SimEventCallback,
  GameOverCallback,
} from '@/types/game';
