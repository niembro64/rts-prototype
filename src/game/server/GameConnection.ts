// GameConnection types - uniform interface for host and remote clients

export type {
  GameConnection,
  SnapshotCallback,
  SnapshotUnsubscribe,
  SimEventCallback,
  GameOverCallback,
} from '@/types/game';
