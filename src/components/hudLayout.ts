export const HUD_MINIMAP_MAX_PX = 272;
export const HUD_MINIMAP_STACK_GAP_PX = 6;
export const HUD_MINIMAP_FOLLOW_TOP_PX = HUD_MINIMAP_MAX_PX + HUD_MINIMAP_STACK_GAP_PX;
/** The top status bar is one band at BAR density: every element — the
 *  resource bands, the compass/sun/wind trio, network status, EXIT — fits
 *  inside this height, so nothing in the bar stacks rows to make room. */
export const HUD_TOP_BAR_HEIGHT_PX = 44;
